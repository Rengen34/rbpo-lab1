# Рабочее состояние лабораторной работы 2

Дата: 2026-09-19. Цель: реализовать вариант 24 «Внутренние закупки организации» по видимой странице `variant_24.pdf`, сохранив лабораторную 1.

Источник: файл `C:\Users\satur\Downloads\variant_24.pdf`, SHA-256 `3BB40D3765BAB5777448181B136E51CF9AC05E67016F0C8894396FFFDF12E87B`. Страница 1 просмотрена как изображение и сопоставлена с текстовым слоем. Требования к реализации зафиксированы в [спецификации v2](spec-v2.md).

Исходное состояние: ветка `lab2` создана от коммита `a26dba8` ветки `main`; приложение первой работы собиралось и проходило четыре теста. Это точка возврата без потери первой работы.

Решение до реализации: пять ресурсов, пять REST-операций для каждого, хранение в памяти, запрет удаления записей со ссылками, серверные `id` и время, контроллеры/сервис/репозиторий. Значения статусов и единицы сроков в задании не уточнены; выбранный контракт описан в спецификации, без дополнительных правил переходов статусов.

Изменения: добавлены пять моделей ресурсов и входные записи, пять контроллеров, `ProcurementService`, хранилище коллекций, серверные часы, обработка ошибок, строгие настройки JSON и локальное прослушивание. Два адреса первой работы не менялись. Контракт и руководство находятся в [спецификации](spec-v2.md), [инструкции](lab2-guide.md) и [проверке безопасности](security-v2.md). Точка возврата без этой работы — `a26dba8`; для опубликованного изменения нужен отдельный обратный коммит, а не удаление истории.

Проверки 19.09.2026:

- `.\mvnw.cmd -B -ntp verify` на Java 21: `BUILD SUCCESS`, 9 тестов, 0 ошибок (4 прежних и 5 новых). Новые HTTP-тесты MockMvc проходят все 25 маршрутов, статусы, частичный PATCH, серверное время, неверный ввод, отсутствующие записи, несовместимые ссылки и запрет удаления связанных записей.
- Собранный JAR запущен отдельно на `127.0.0.1:18080`. Через сеть проверены прежний адрес, пустой список, создание пользователя, заявки, поставщика, предложения и заказа, частичное изменение, конфликт удаления предложения, затем удаление в правильном порядке. Последовательность статусов: `200,200,201,201,201,201,201,200,409,204,204,204,204,204,200`; ожидаемая совпала с фактической. `Get-NetTCPConnection` показал локальное прослушивание `127.0.0.1:18080`. Процесс завершён.
- Отдельный проверяющий запустил JAR на `127.0.0.1:18082`: десятичное число сохранило точность при POST, а PATCH с явным `null` дал 400. Подтверждённых замечаний по корректности, целостности, обработке ошибок и читаемости в просмотренном коде нет.
- В отдельной копии проекта проверено расширение из руководства: поле `department` добавлено в модели, ввод, сервис и HTTP-тест без изменения контроллера и хранилища. `verify` завершился успешно (9 тестов); через локальный HTTP проверены POST 201, PATCH 200, GET 200 и неверный тип 400. Копия не переносилась в основной проект. Уточнено ограничение: при запрете явного `null` новое необязательное поле нельзя очистить без отдельного правила.

Ограничения: хранение только в памяти, аутентификации и прав доступа нет, поэтому сервис годится только для локального учебного использования с вымышленными данными. Конкурентная нагрузка и все 25 маршрутов через отдельный сетевой процесс не проверялись; все маршруты прошли MockMvc. Отдельный проход руководства новым разработчиком за 30–60 минут не проводился; расширение выше было технической самопроверкой. Процедура публичного отката также не проверялась. Отчёт создан отдельно в папке «Отчёт» и визуально проверен. Следующее действие: зафиксировать это уточнение и отправить проверенные исходники.
